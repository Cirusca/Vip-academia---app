# Fase 2C Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar dívidas da Fase 1/2: callbackUrl do login, fluxo mustChangePassword/troca de senha, limpeza de /configuracoes, ESLint e branding.

**Architecture:** Pipeline de auth Auth.js v5 estendido com `mustChangePassword` (JWT → session → middleware gate → página forçada); troca de senha voluntária fiada na aba de segurança de /configuracoes; todo código novo segue server-only + TDD.

**Tech Stack:** Next.js 16 / Auth.js v5 / Prisma 7 / Zod / bcryptjs / React 19 (`useTransition`)

**Branch:** `claude/requirements-report-plan-bz1qad`

---

## Mapa de arquivos

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `app/login/page.tsx` | Modificar | Passar `callbackUrl` como `redirectTo` ao signIn |
| `auth.ts` | Modificar | Incluir `mustChangePassword` no objeto retornado pelo `authorize` |
| `auth.config.ts` | Modificar | Propagar `mustChangePassword` no JWT callback, session callback e gate `authorized` |
| `types/next-auth.d.ts` | Modificar | Adicionar `mustChangePassword: boolean` em `Session.user` e `User` |
| `lib/auth/session.ts` | Modificar | Adicionar `mustChangePassword: boolean` em `SessionUser` e `getSession` |
| `lib/validation/user.ts` | Modificar | Adicionar `changePasswordSchema` e `forceChangePasswordSchema` |
| `lib/data/users.ts` | Criar | `changePassword()` server-only com lógica mustChangePassword |
| `lib/data/users.integration.test.ts` | Criar | Testes de integração TDD para changePassword |
| `app/actions/user.ts` | Criar | `changePasswordAction` (retorna `{error?}`, nunca lança) |
| `app/trocar-senha/page.tsx` | Criar | Página de troca forçada (mustChangePassword=true) |
| `app/configuracoes/page.tsx` | Modificar | Server Component fino que lê sessão e passa props |
| `app/configuracoes/client.tsx` | Criar | Client Component: 3 abas reais (perfil RO, segurança, aparência) |
| `.eslintrc.json` | Criar | Config ESLint mínima |
| `public/manifest.json` | Modificar | Branding: FitPro → VIP Academia |
| `app/layout.tsx` | Modificar | Branding: título e descrição |
| `docs/HANDOFF.md` | Modificar | Documenta Fase 2C + leitura de segurança |

---

## Task 1: callbackUrl fix no login

**Files:**
- Modify: `app/login/page.tsx`

**Problema:** `signIn("credentials", { redirectTo: "/" })` ignora o `?callbackUrl=` que o middleware injeta ao redirecionar para `/login`.

**Fix:** ler `callbackUrl` dos searchParams, inserir como `<input type="hidden">` no form, usar no `redirectTo`.

- [ ] **Step 1: Editar `app/login/page.tsx`**

```tsx
// app/login/page.tsx
import { AuthError } from "next-auth"
import { redirect } from "next/navigation"
import { Dumbbell } from "lucide-react"
import { signIn } from "@/auth"

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string; callbackUrl?: string }>
}) {
  const { error, callbackUrl } = await searchParams
  const redirectTo = callbackUrl ?? "/"

  async function authenticate(formData: FormData) {
    "use server"
    try {
      await signIn("credentials", {
        email: formData.get("email"),
        password: formData.get("password"),
        redirectTo: (formData.get("callbackUrl") as string | null) ?? "/",
      })
    } catch (err) {
      if (err instanceof AuthError) {
        redirect("/login?error=1")
      }
      throw err
    }
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-4">
      <div className="w-full max-w-sm rounded-2xl border border-border bg-card p-8 shadow-sm">
        <div className="mb-6 flex flex-col items-center gap-3 text-center">
          <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-primary">
            <Dumbbell className="h-7 w-7 text-primary-foreground" />
          </div>
          <div>
            <h1 className="text-xl font-bold text-foreground">VIP Academia</h1>
            <p className="text-sm text-muted-foreground">Entre para continuar</p>
          </div>
        </div>

        {error && (
          <p
            role="alert"
            className="mb-4 rounded-lg bg-destructive/10 px-3 py-2 text-sm text-destructive"
          >
            E-mail ou senha inválidos.
          </p>
        )}

        <form action={authenticate} className="space-y-4">
          <input type="hidden" name="callbackUrl" value={redirectTo} />
          <div className="space-y-1.5">
            <label htmlFor="email" className="text-sm font-medium text-foreground">
              E-mail
            </label>
            <input
              id="email"
              name="email"
              type="email"
              autoComplete="email"
              required
              className="w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none focus:ring-2 focus:ring-primary"
            />
          </div>

          <div className="space-y-1.5">
            <label htmlFor="password" className="text-sm font-medium text-foreground">
              Senha
            </label>
            <input
              id="password"
              name="password"
              type="password"
              autoComplete="current-password"
              required
              className="w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none focus:ring-2 focus:ring-primary"
            />
          </div>

          <button
            type="submit"
            className="w-full rounded-lg bg-primary px-3 py-2.5 text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
          >
            Entrar
          </button>
        </form>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Verificar que o TypeScript compila**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | head -20
```

Esperado: sem erros relacionados a `login/page.tsx`.

- [ ] **Step 3: Commit**

```bash
git add app/login/page.tsx
git commit -m "fix(login): respeitar callbackUrl ao redirecionar após login"
```

---

## Task 2: mustChangePassword no pipeline de auth

**Files:**
- Modify: `auth.ts` (linha 50-56: objeto retornado pelo authorize)
- Modify: `auth.config.ts` (TokenClaims, jwt callback, session callback, authorized gate)
- Modify: `types/next-auth.d.ts`
- Modify: `lib/auth/session.ts`

Sem testes unitários — é configuração pura; o comportamento é validado pela Task 4 (data layer) e pela Task 6 (página /trocar-senha).

- [ ] **Step 1: Editar `auth.ts` — retornar `mustChangePassword` do `authorize`**

Localizar o bloco `return {` na linha ~50 e adicionar o campo:

```ts
// dentro do Credentials authorize, substituir o return existente por:
return {
  id: user.id,
  email: user.email,
  name: user.name,
  gymId: user.gymId,
  roles: user.roles,
  mustChangePassword: user.mustChangePassword,
}
```

- [ ] **Step 2: Editar `auth.config.ts` — estender TokenClaims + propagar + gate**

Substituir o arquivo inteiro por:

```ts
import type { NextAuthConfig } from "next-auth"
import type { Role } from "@/lib/generated/prisma/enums"

type TokenClaims = {
  userId?: string
  gymId?: string
  roles?: Role[]
  mustChangePassword?: boolean
}

export const authConfig = {
  trustHost: true,
  pages: { signIn: "/login" },
  session: { strategy: "jwt" },
  providers: [],
  callbacks: {
    authorized({ auth, request }) {
      const isLoggedIn = !!auth?.user
      const path = request.nextUrl.pathname
      const isOnLogin = path.startsWith("/login")
      const isOnTrocarSenha = path.startsWith("/trocar-senha")

      if (!isLoggedIn) {
        if (isOnLogin) return true
        return false
      }

      // Usuário autenticado
      if (isOnLogin) return Response.redirect(new URL("/", request.nextUrl))

      if (auth.user.mustChangePassword && !isOnTrocarSenha) {
        return Response.redirect(new URL("/trocar-senha", request.nextUrl))
      }

      return true
    },

    jwt({ token, user }) {
      if (user) {
        const claims = token as TokenClaims
        claims.userId = user.id
        claims.gymId = user.gymId
        claims.roles = user.roles
        claims.mustChangePassword = user.mustChangePassword
      }
      return token
    },

    session({ session, token }) {
      const claims = token as TokenClaims
      if (session.user && claims.userId) {
        session.user.id = claims.userId
        session.user.gymId = claims.gymId ?? ""
        session.user.roles = claims.roles ?? []
        session.user.mustChangePassword = claims.mustChangePassword ?? false
      }
      return session
    },
  },
} satisfies NextAuthConfig
```

- [ ] **Step 3: Editar `types/next-auth.d.ts` — adicionar mustChangePassword**

```ts
import type { Role } from "@/lib/generated/prisma/enums"
import type { DefaultSession } from "next-auth"

declare module "next-auth" {
  interface Session {
    user: {
      id: string
      gymId: string
      roles: Role[]
      mustChangePassword: boolean
    } & DefaultSession["user"]
  }

  interface User {
    gymId: string
    roles: Role[]
    mustChangePassword: boolean
  }
}
```

- [ ] **Step 4: Editar `lib/auth/session.ts` — adicionar mustChangePassword em SessionUser e getSession**

```ts
import "server-only"

import type { Role } from "@/lib/generated/prisma/enums"
import { auth } from "@/auth"
import { NotAuthenticatedError } from "@/lib/auth/errors"

export interface SessionUser {
  userId: string
  gymId: string
  roles: Role[]
  mustChangePassword: boolean
}

async function getSession(): Promise<SessionUser | null> {
  const session = await auth()
  if (!session?.user?.id) return null
  return {
    userId: session.user.id,
    gymId: session.user.gymId,
    roles: session.user.roles,
    mustChangePassword: session.user.mustChangePassword ?? false,
  }
}

export async function requireSession(): Promise<SessionUser> {
  const session = await getSession()
  if (!session) {
    throw new NotAuthenticatedError()
  }
  return session
}

export function isProfissional(session: SessionUser): boolean {
  return session.roles.includes("profissional")
}

export function isAluno(session: SessionUser): boolean {
  return session.roles.includes("aluno")
}
```

- [ ] **Step 5: Verificar build do TypeScript**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | head -30
```

Esperado: 0 erros (ou só erros em outros arquivos, não nos 4 modificados acima).

- [ ] **Step 6: Rodar testes existentes — não devem quebrar**

```bash
cd /home/user/Vip-academia---app && pnpm test 2>&1 | tail -20
```

Esperado: ainda 68 passando.

- [ ] **Step 7: Commit**

```bash
git add auth.ts auth.config.ts types/next-auth.d.ts lib/auth/session.ts
git commit -m "feat(auth): propagar mustChangePassword no JWT + gate de rota no middleware"
```

---

## Task 3: Schemas Zod para troca de senha

**Files:**
- Modify: `lib/validation/user.ts`

- [ ] **Step 1: Escrever teste unitário vermelhoide de validation**

Os schemas são tão simples que os testes de integração (Task 4) os exercitam suficientemente. Pule testes unitários de schema aqui (YAGNI) — a lógica relevante é testada na camada de dados.

- [ ] **Step 2: Adicionar schemas no final de `lib/validation/user.ts`**

```ts
// Adicionar após as exports existentes:

/**
 * Troca voluntária de senha (aba de segurança em /configuracoes).
 * Exige senha atual para não abrir vetor de session-hijacking.
 */
export const changePasswordSchema = z
  .object({
    currentPassword: z.string().min(1, "Senha atual obrigatória."),
    newPassword: passwordSchema,
    confirmPassword: z.string(),
  })
  .refine((d) => d.newPassword === d.confirmPassword, {
    message: "As senhas não coincidem.",
    path: ["confirmPassword"],
  })

export type ChangePasswordInput = z.infer<typeof changePasswordSchema>

/**
 * Troca forçada de senha (mustChangePassword=true → /trocar-senha).
 * Não exige senha atual — o profissional admin já redefiniu no backend.
 */
export const forceChangePasswordSchema = z
  .object({
    newPassword: passwordSchema,
    confirmPassword: z.string(),
  })
  .refine((d) => d.newPassword === d.confirmPassword, {
    message: "As senhas não coincidem.",
    path: ["confirmPassword"],
  })

export type ForceChangePasswordInput = z.infer<typeof forceChangePasswordSchema>
```

- [ ] **Step 3: Commit**

```bash
git add lib/validation/user.ts
git commit -m "feat(validation): adicionar changePasswordSchema + forceChangePasswordSchema"
```

---

## Task 4: lib/data/users.ts — TDD

**Files:**
- Create: `lib/data/users.ts`
- Create: `lib/data/users.integration.test.ts`

### TDD Red — escrever testes primeiro

- [ ] **Step 1: Criar `lib/data/users.integration.test.ts`**

```ts
import { describe, it, expect, beforeAll, afterAll } from "vitest"
import bcrypt from "bcryptjs"
import { db } from "@/lib/data/_scope"
import { changePassword } from "@/lib/data/users"
import { NotFoundError } from "@/lib/auth/errors"
import type { SessionUser } from "@/lib/auth/session"

const BASE_PW = "senha123A"

// Utilitário de sessão de teste
function makeSession(overrides: Partial<SessionUser> & { userId: string; gymId: string }): SessionUser {
  return { roles: ["profissional"], mustChangePassword: false, ...overrides }
}

describe("changePassword", () => {
  let userId: string
  const gymId = `test-users-${Date.now()}`

  beforeAll(async () => {
    const hash = await bcrypt.hash(BASE_PW, 10)
    const user = await db.user.create({
      data: {
        email: `user-chpw-${Date.now()}@test.dev`,
        name: "Teste ChangePassword",
        gymId,
        roles: ["profissional"],
        passwordHash: hash,
        status: "ativo",
        mustChangePassword: false,
      },
      select: { id: true },
    })
    userId = user.id
  })

  afterAll(async () => {
    await db.user.deleteMany({ where: { gymId } })
  })

  it("atualiza o hash e mantém mustChangePassword=false", async () => {
    const session = makeSession({ userId, gymId })
    await changePassword(session, { currentPassword: BASE_PW, newPassword: "novo456B" })

    const u = await db.user.findUnique({ where: { id: userId }, select: { passwordHash: true, mustChangePassword: true } })
    expect(await bcrypt.compare("novo456B", u!.passwordHash)).toBe(true)
    expect(u!.mustChangePassword).toBe(false)

    // Restaurar para os próximos testes
    const hash = await bcrypt.hash(BASE_PW, 10)
    await db.user.update({ where: { id: userId }, data: { passwordHash: hash } })
  })

  it("lança erro com senha atual incorreta", async () => {
    const session = makeSession({ userId, gymId })
    await expect(
      changePassword(session, { currentPassword: "ERRADA!1", newPassword: "outro456B" })
    ).rejects.toThrow("Senha atual incorreta")
  })

  it("pula verificação de senha atual quando mustChangePassword=true", async () => {
    await db.user.update({ where: { id: userId }, data: { mustChangePassword: true } })
    const session = makeSession({ userId, gymId, mustChangePassword: true })
    await expect(
      changePassword(session, { newPassword: "forca789C" })
    ).resolves.toBeUndefined()

    const u = await db.user.findUnique({ where: { id: userId }, select: { mustChangePassword: true } })
    expect(u!.mustChangePassword).toBe(false)

    // Restaurar hash para próximos testes
    const hash = await bcrypt.hash(BASE_PW, 10)
    await db.user.update({ where: { id: userId }, data: { passwordHash: hash } })
  })

  it("lança NotFoundError para acesso cross-tenant", async () => {
    const wrongSession = makeSession({ userId, gymId: "outro-gym-inexistente" })
    await expect(
      changePassword(wrongSession, { currentPassword: BASE_PW, newPassword: "hack123B" })
    ).rejects.toThrow(NotFoundError)
  })
})
```

- [ ] **Step 2: Rodar — deve falhar com "Cannot find module"**

```bash
cd /home/user/Vip-academia---app && pnpm test lib/data/users.integration.test.ts 2>&1 | tail -15
```

Esperado: FAIL — `Error: Cannot find module '@/lib/data/users'`.

### TDD Green — implementar

- [ ] **Step 3: Criar `lib/data/users.ts`**

```ts
import "server-only"

import bcrypt from "bcryptjs"
import { db, tenantWhere } from "@/lib/data/_scope"
import { NotFoundError } from "@/lib/auth/errors"
import type { SessionUser } from "@/lib/auth/session"

/**
 * Troca a senha do usuário autenticado.
 *
 * Se session.mustChangePassword=true → pula verificação da senha atual (o admin
 * já redefiniu; o usuário só precisa confirmar a nova). Caso contrário, exige e
 * verifica a senha atual antes de gravar a nova (anti-session-hijacking).
 *
 * Em ambos os casos: define mustChangePassword=false após a troca.
 */
export async function changePassword(
  session: SessionUser,
  input: { currentPassword?: string; newPassword: string },
): Promise<void> {
  const user = await db.user.findFirst({
    where: tenantWhere(session, { id: session.userId }),
    select: { id: true, passwordHash: true, mustChangePassword: true },
  })
  if (!user) throw new NotFoundError()

  if (!user.mustChangePassword) {
    if (!input.currentPassword) throw new Error("Senha atual obrigatória.")
    const ok = await bcrypt.compare(input.currentPassword, user.passwordHash)
    if (!ok) throw new Error("Senha atual incorreta.")
  }

  const hash = await bcrypt.hash(input.newPassword, 12)
  await db.user.update({
    where: { id: user.id },
    data: { passwordHash: hash, mustChangePassword: false },
  })
}
```

- [ ] **Step 4: Rodar testes — todos devem passar**

```bash
cd /home/user/Vip-academia---app && pnpm test lib/data/users.integration.test.ts 2>&1 | tail -20
```

Esperado: 4 passed.

- [ ] **Step 5: Rodar suite completa — 72 testes**

```bash
cd /home/user/Vip-academia---app && pnpm test 2>&1 | tail -10
```

Esperado: 72 passed (68 anteriores + 4 novos).

- [ ] **Step 6: Commit**

```bash
git add lib/data/users.ts lib/data/users.integration.test.ts
git commit -m "test+feat(users): TDD changePassword — 4 testes de integração + implementação"
```

---

## Task 5: Server Action changePasswordAction

**Files:**
- Create: `app/actions/user.ts`

A action retorna `{ error?: string }` em vez de lançar — padrão que permite ao client component ler o resultado sem precisar de try/catch de erros de rede.

- [ ] **Step 1: Criar `app/actions/user.ts`**

```ts
"use server"

import { z } from "zod"
import { requireSession } from "@/lib/auth/session"
import { changePasswordSchema } from "@/lib/validation/user"
import { changePassword } from "@/lib/data/users"

/**
 * Troca de senha voluntária (/configuracoes → aba Segurança).
 * Retorna { error } em vez de lançar para o client component lidar com o estado.
 */
export async function changePasswordAction(raw: unknown): Promise<{ error?: string }> {
  try {
    const session = await requireSession()
    const input = changePasswordSchema.parse(raw)
    await changePassword(session, {
      currentPassword: input.currentPassword,
      newPassword: input.newPassword,
    })
    return {}
  } catch (err) {
    if (err instanceof z.ZodError) return { error: err.errors[0]?.message ?? "Dados inválidos." }
    if (err instanceof Error) return { error: err.message }
    return { error: "Erro ao alterar senha." }
  }
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | grep "app/actions/user"
```

Esperado: sem output (nenhum erro no arquivo).

- [ ] **Step 3: Commit**

```bash
git add app/actions/user.ts
git commit -m "feat(actions): adicionar changePasswordAction (troca voluntária de senha)"
```

---

## Task 6: Página /trocar-senha (mustChangePassword forçado)

**Files:**
- Create: `app/trocar-senha/page.tsx`

Fluxo: usuário com `mustChangePassword=true` é bloqueado pelo middleware em qualquer rota exceto `/trocar-senha`. Após trocar a senha, `signOut` limpa o JWT (que ainda tem `mustChangePassword=true`). No próximo login, o DB já tem `mustChangePassword=false` e o JWT novo reflete isso.

- [ ] **Step 1: Criar `app/trocar-senha/page.tsx`**

```tsx
import { redirect } from "next/navigation"
import { Dumbbell } from "lucide-react"
import { signOut } from "@/auth"
import { requireSession } from "@/lib/auth/session"
import { forceChangePasswordSchema } from "@/lib/validation/user"
import { changePassword } from "@/lib/data/users"

export default async function TrocarSenhaPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>
}) {
  const session = await requireSession()
  // Sem gate de mustChangePassword: se o usuário chegar aqui sem precisar,
  // apenas mostramos a página (o middleware já filtra quem deve estar aqui).

  const { error } = await searchParams

  async function action(formData: FormData) {
    "use server"
    const sess = await requireSession()
    const parsed = forceChangePasswordSchema.safeParse({
      newPassword: formData.get("newPassword"),
      confirmPassword: formData.get("confirmPassword"),
    })
    if (!parsed.success) {
      redirect(`/trocar-senha?error=${encodeURIComponent(parsed.error.errors[0]?.message ?? "Dados inválidos.")}`)
    }
    try {
      await changePassword(sess, { newPassword: parsed.data.newPassword })
    } catch {
      redirect("/trocar-senha?error=Erro+ao+alterar+senha.")
    }
    await signOut({ redirectTo: "/login" })
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-background p-4">
      <div className="w-full max-w-sm rounded-2xl border border-border bg-card p-8 shadow-sm">
        <div className="mb-6 flex flex-col items-center gap-3 text-center">
          <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-primary">
            <Dumbbell className="h-7 w-7 text-primary-foreground" />
          </div>
          <div>
            <h1 className="text-xl font-bold text-foreground">Defina sua senha</h1>
            <p className="text-sm text-muted-foreground">
              Seu acesso requer uma nova senha antes de continuar.
            </p>
          </div>
        </div>

        {error && (
          <p
            role="alert"
            className="mb-4 rounded-lg bg-destructive/10 px-3 py-2 text-sm text-destructive"
          >
            {decodeURIComponent(error)}
          </p>
        )}

        <form action={action} className="space-y-4">
          <div className="space-y-1.5">
            <label htmlFor="newPassword" className="text-sm font-medium text-foreground">
              Nova senha
            </label>
            <input
              id="newPassword"
              name="newPassword"
              type="password"
              autoComplete="new-password"
              required
              className="w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none focus:ring-2 focus:ring-primary"
            />
          </div>

          <div className="space-y-1.5">
            <label htmlFor="confirmPassword" className="text-sm font-medium text-foreground">
              Confirmar senha
            </label>
            <input
              id="confirmPassword"
              name="confirmPassword"
              type="password"
              autoComplete="new-password"
              required
              className="w-full rounded-lg border border-border bg-background px-3 py-2.5 text-sm text-foreground outline-none focus:ring-2 focus:ring-primary"
            />
          </div>

          <button
            type="submit"
            className="w-full rounded-lg bg-primary px-3 py-2.5 text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
          >
            Definir senha e entrar
          </button>
        </form>
      </div>
    </main>
  )
}
```

> Nota: `session` lido duas vezes (page + action) — padrão do Next.js com Server Actions inline. O `requireSession` no escopo da página garante que a ação receba a sessão correta mesmo que o cookie expire entre os dois calls.

- [ ] **Step 2: Verificar TypeScript**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | grep "trocar-senha"
```

Esperado: sem output.

- [ ] **Step 3: Commit**

```bash
git add app/trocar-senha/page.tsx
git commit -m "feat(trocar-senha): página de troca forçada de senha (mustChangePassword flow)"
```

---

## Task 7: /configuracoes cleanup

**Files:**
- Modify: `app/configuracoes/page.tsx` (→ server component fino)
- Create: `app/configuracoes/client.tsx` (client component: 3 abas reais)

Remover: tabs "Academia" (mock admin) + "Notificações" (mock). Manter: Perfil (read-only da sessão), Segurança (form real via `changePasswordAction`), Aparência (ThemeProvider via `useTheme`).

- [ ] **Step 1: Substituir `app/configuracoes/page.tsx` pelo server wrapper**

```tsx
import { redirect } from "next/navigation"
import { requireSession } from "@/lib/auth/session"
import ConfiguracoesClient from "./client"

export default async function ConfiguracoesPage() {
  const session = await requireSession()
  return (
    <ConfiguracoesClient
      name={session.userId} // placeholder até Profile model existir
    />
  )
}
```

Obs.: `session.userId` é temporário — `name` real virá com o model `Profile` na Fase 4. Por ora mostramos o userId como fallback (ou lemos o `name` do User direto se necessário — ver nota abaixo).

> Ponytail: se precisar do `name` real do User, adicionar uma query `db.user.findFirst({ where: { id: session.userId }, select: { name: true } })` no server component. Mas para não adicionar uma query extra, usar a session. O campo `name` está disponível em `auth().user.name` — let's read via `auth()` diretamente:

```tsx
import { auth } from "@/auth"
import { redirect } from "next/navigation"
import { requireSession } from "@/lib/auth/session"
import ConfiguracoesClient from "./client"

export default async function ConfiguracoesPage() {
  const session = await requireSession()
  const raw = await auth()
  return (
    <ConfiguracoesClient
      name={raw?.user?.name ?? ""}
      email={raw?.user?.email ?? ""}
      roles={session.roles}
    />
  )
}
```

- [ ] **Step 2: Criar `app/configuracoes/client.tsx`**

```tsx
"use client"

import { useState, useTransition } from "react"
import { useTheme } from "next-themes"
import { AppLayout } from "@/components/app-layout"
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Switch } from "@/components/ui/switch"
import { User, Shield, Palette, Key, Moon, Sun, Globe } from "lucide-react"
import { changePasswordAction } from "@/app/actions/user"
import type { Role } from "@/lib/generated/prisma/enums"

interface Props {
  name: string
  email: string
  roles: Role[]
}

const tabs = [
  { id: "profile", label: "Perfil", icon: User },
  { id: "security", label: "Segurança", icon: Shield },
  { id: "appearance", label: "Aparência", icon: Palette },
]

export default function ConfiguracoesClient({ name, email, roles }: Props) {
  const [activeTab, setActiveTab] = useState("profile")
  const { theme, setTheme } = useTheme()
  const [isPending, startTransition] = useTransition()
  const [pwError, setPwError] = useState("")
  const [pwSuccess, setPwSuccess] = useState(false)

  function handlePasswordSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    const fd = new FormData(e.currentTarget)
    const form = e.currentTarget
    setPwError("")
    setPwSuccess(false)
    startTransition(async () => {
      const result = await changePasswordAction({
        currentPassword: fd.get("currentPassword"),
        newPassword: fd.get("newPassword"),
        confirmPassword: fd.get("confirmPassword"),
      })
      if (result.error) {
        setPwError(result.error)
      } else {
        setPwSuccess(true)
        form.reset()
      }
    })
  }

  const renderContent = () => {
    switch (activeTab) {
      case "profile":
        return (
          <Card className="bg-card border-border">
            <CardHeader>
              <CardTitle className="text-foreground">Perfil</CardTitle>
              <CardDescription>Suas informações de conta (somente leitura)</CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              <div className="space-y-1.5">
                <label className="text-sm font-medium text-foreground">Nome</label>
                <Input value={name} disabled className="bg-secondary border-border" />
              </div>
              <div className="space-y-1.5">
                <label className="text-sm font-medium text-foreground">E-mail</label>
                <Input value={email} disabled className="bg-secondary border-border" />
              </div>
              <div className="space-y-1.5">
                <label className="text-sm font-medium text-foreground">Papéis</label>
                <Input value={roles.join(", ")} disabled className="bg-secondary border-border" />
              </div>
              <p className="text-xs text-muted-foreground">
                Edição de perfil disponível na Fase 4.
              </p>
            </CardContent>
          </Card>
        )

      case "security":
        return (
          <Card className="bg-card border-border">
            <CardHeader>
              <CardTitle className="text-foreground">Segurança</CardTitle>
              <CardDescription>Altere sua senha de acesso</CardDescription>
            </CardHeader>
            <CardContent>
              <form onSubmit={handlePasswordSubmit} className="space-y-4">
                <div className="flex items-center gap-3 mb-2">
                  <div className="rounded-lg bg-primary/20 p-2">
                    <Key className="h-5 w-5 text-primary" />
                  </div>
                  <p className="font-medium text-foreground">Alterar Senha</p>
                </div>

                {pwError && (
                  <p role="alert" className="rounded-lg bg-destructive/10 px-3 py-2 text-sm text-destructive">
                    {pwError}
                  </p>
                )}
                {pwSuccess && (
                  <p role="status" className="rounded-lg bg-green-500/10 px-3 py-2 text-sm text-green-600">
                    Senha alterada com sucesso.
                  </p>
                )}

                <div className="space-y-1.5">
                  <label htmlFor="currentPassword" className="text-sm font-medium text-foreground">
                    Senha atual
                  </label>
                  <Input
                    id="currentPassword"
                    name="currentPassword"
                    type="password"
                    required
                    className="bg-secondary border-border"
                  />
                </div>
                <div className="space-y-1.5">
                  <label htmlFor="newPassword" className="text-sm font-medium text-foreground">
                    Nova senha
                  </label>
                  <Input
                    id="newPassword"
                    name="newPassword"
                    type="password"
                    autoComplete="new-password"
                    required
                    className="bg-secondary border-border"
                  />
                </div>
                <div className="space-y-1.5">
                  <label htmlFor="confirmPassword" className="text-sm font-medium text-foreground">
                    Confirmar nova senha
                  </label>
                  <Input
                    id="confirmPassword"
                    name="confirmPassword"
                    type="password"
                    autoComplete="new-password"
                    required
                    className="bg-secondary border-border"
                  />
                </div>
                <Button
                  type="submit"
                  disabled={isPending}
                  className="bg-primary text-primary-foreground"
                >
                  {isPending ? "Salvando..." : "Alterar Senha"}
                </Button>
              </form>
            </CardContent>
          </Card>
        )

      case "appearance":
        return (
          <Card className="bg-card border-border">
            <CardHeader>
              <CardTitle className="text-foreground">Aparência</CardTitle>
              <CardDescription>Tema da interface</CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              <div className="flex items-center justify-between rounded-lg bg-secondary/30 p-4">
                <div className="flex items-center gap-4">
                  <div className="rounded-lg bg-primary/20 p-2">
                    {theme === "dark" ? (
                      <Moon className="h-5 w-5 text-primary" />
                    ) : (
                      <Sun className="h-5 w-5 text-primary" />
                    )}
                  </div>
                  <div>
                    <p className="font-medium text-foreground">Modo Escuro</p>
                    <p className="text-sm text-muted-foreground">
                      {theme === "dark" ? "Ativado" : "Desativado"}
                    </p>
                  </div>
                </div>
                <Switch
                  checked={theme === "dark"}
                  onCheckedChange={(v) => setTheme(v ? "dark" : "light")}
                />
              </div>
              <div className="flex items-center justify-between rounded-lg bg-secondary/30 p-4">
                <div className="flex items-center gap-4">
                  <div className="rounded-lg bg-primary/20 p-2">
                    <Globe className="h-5 w-5 text-primary" />
                  </div>
                  <div>
                    <p className="font-medium text-foreground">Idioma</p>
                    <p className="text-sm text-muted-foreground">Português (Brasil)</p>
                  </div>
                </div>
              </div>
            </CardContent>
          </Card>
        )

      default:
        return null
    }
  }

  return (
    <AppLayout
      title="Configurações"
      subtitle="Preferências da sua conta"
    >
      <div className="grid gap-6 lg:grid-cols-4">
        <Card className="bg-card border-border lg:col-span-1 h-fit">
          <CardContent className="p-2">
            <nav className="space-y-1">
              {tabs.map((tab) => {
                const Icon = tab.icon
                const isActive = activeTab === tab.id
                return (
                  <button
                    key={tab.id}
                    onClick={() => setActiveTab(tab.id)}
                    className={`w-full flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium transition-colors ${
                      isActive
                        ? "bg-primary/20 text-primary"
                        : "text-muted-foreground hover:bg-secondary hover:text-foreground"
                    }`}
                  >
                    <Icon className="h-4 w-4" />
                    {tab.label}
                  </button>
                )
              })}
            </nav>
          </CardContent>
        </Card>

        <div className="lg:col-span-3">{renderContent()}</div>
      </div>
    </AppLayout>
  )
}
```

- [ ] **Step 3: Verificar TypeScript + testes**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | head -30
```

```bash
cd /home/user/Vip-academia---app && pnpm test 2>&1 | tail -10
```

Esperado: 0 erros TS; 72 testes passando.

- [ ] **Step 4: Commit**

```bash
git add app/configuracoes/page.tsx app/configuracoes/client.tsx
git commit -m "feat(configuracoes): limpar abas mock, wiring real de troca de senha + tema"
```

---

## Task 8: ESLint

**Files:**
- Create: `.eslintrc.json`

- [ ] **Step 1: Criar `.eslintrc.json`**

```json
{
  "extends": "next/core-web-vitals"
}
```

- [ ] **Step 2: Rodar lint**

```bash
cd /home/user/Vip-academia---app && pnpm lint 2>&1 | tail -20
```

Esperado: 0 errors (podem existir warnings de acessibilidade — aceitáveis no MVP).

- [ ] **Step 3: Commit**

```bash
git add .eslintrc.json
git commit -m "chore(eslint): adicionar config mínima next/core-web-vitals"
```

---

## Task 9: Branding

**Files:**
- Modify: `public/manifest.json`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Editar `public/manifest.json` — FitPro → VIP Academia**

Localizar os campos `"name"` e `"short_name"` e atualizar:

```json
{
  "name": "VIP Academia",
  "short_name": "VIP",
  ...
}
```

Também atualizar qualquer ocorrência de "FitPro" no arquivo.

- [ ] **Step 2: Editar `app/layout.tsx` — title + description + authors**

Localizar o objeto `metadata` e atualizar:

```ts
export const metadata: Metadata = {
  title: 'VIP Academia',
  description: 'App de treino B2B2C para academias — profissional atribui, aluno executa.',
  generator: 'next',
  keywords: ['academia', 'treinos', 'fitness', 'personal trainer'],
  authors: [{ name: 'VIP Academia' }],
  manifest: '/manifest.json',
  appleWebApp: {
    // manter o que já existia
  },
}
```

Remover o bloco de comentário gigante do cabeçalho (`/* === FITPRO ACADEMIA === */`) — é ruído, não documentação.

- [ ] **Step 3: Verificar build**

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | head -10
```

Esperado: 0 erros.

- [ ] **Step 4: Commit**

```bash
git add public/manifest.json app/layout.tsx
git commit -m "chore(branding): renomear FitPro → VIP Academia em manifest e layout"
```

---

## Task 10: Leitura de segurança Fase 2C + HANDOFF + push

**Files:**
- Modify: `docs/HANDOFF.md`

### Checklist de segurança Fase 2C

Verificar cada item e registrar no HANDOFF:

- [ ] **`mustChangePassword` nunca vem do cliente:** campo lido do DB no `authorize` → baked no JWT → lido pelo servidor. O input do cliente (formData) nunca contém `mustChangePassword`. ✅
- [ ] **`currentPassword` não pode ser bypassado voluntariamente:** `changePasswordAction` usa `changePasswordSchema` que exige `currentPassword`. O bypass de `mustChangePassword` só ocorre quando `session.mustChangePassword===true` (lido do JWT, não do cliente). ✅
- [ ] **`signOut` após force-change limpa JWT desatualizado:** sem `signOut`, o JWT permaneceria com `mustChangePassword=true` até expirar. O `signOut` em `/trocar-senha` limpa o cookie imediatamente. ✅
- [ ] **`tenantWhere` em `changePassword`:** a query busca `{ id: session.userId, gymId: session.gymId }` — um user de outro gym que tentar este endpoint recebe `NotFoundError` (anti-IDOR). ✅
- [ ] **`server-only` mantido em `lib/data/users.ts`:** qualquer import acidental no front quebra o build. ✅
- [ ] **`callbackUrl` não abre open redirect:** `formData.get("callbackUrl")` é passado diretamente ao `signIn` como `redirectTo`. Auth.js v5 valida que o redirect fica no mesmo domínio (origem) — open redirect já bloqueado pelo framework. ✅ Dívida: confirmar em produção com `trustHost: true`.

- [ ] **Atualizar `docs/HANDOFF.md`** com nova seção Fase 2C e marcar §C como CONCLUÍDO.

```markdown
### Fase 2 — fatia C: callbackUrl + mustChangePassword + /configuracoes + ESLint + branding (✅ feito + validado)
- **callbackUrl fix:** `app/login/page.tsx` lê `?callbackUrl=` dos searchParams e repassa ao `signIn` como `redirectTo`. Antes, toda autenticação caía em `/` ignorando a rota original.
- **mustChangePassword flow:** campo propagado em `auth.ts → auth.config.ts → types/next-auth.d.ts → lib/auth/session.ts`. Middleware gate em `authorized()` redireciona para `/trocar-senha` enquanto `mustChangePassword=true`. Após troca: `signOut` limpa JWT desatualizado.
- **`lib/data/users.ts`:** `changePassword()` (server-only) — se `mustChangePassword=true`, pula verificação da senha atual; caso contrário exige e verifica via bcrypt. `tenantWhere` + `NotFoundError` (anti-IDOR). TDD: 4 testes de integração.
- **`changePasswordAction`:** retorna `{ error? }` em vez de lançar — permite client component ler o resultado sem try/catch de erros de rede.
- **`/trocar-senha`:** página standalone (igual ao `/login`), sem AppLayout. Inline server action: parse com `forceChangePasswordSchema` → `changePassword()` → `signOut`. Validação no servidor, não no cliente.
- **`/configuracoes` cleanup:** `page.tsx` virou server component fino (lê sessão + passa props). `client.tsx` tem 3 abas reais: Perfil (read-only da sessão), Segurança (form real → `changePasswordAction`), Aparência (`useTheme` do ThemeProvider). Removidos: aba Academia (admin mock), aba Notificações (mock), 2FA placeholder, sessões ativas mock.
- **ESLint:** `.eslintrc.json` com `next/core-web-vitals`.
- **Branding:** `public/manifest.json` + `app/layout.tsx` — "FitPro Academia" → "VIP Academia".
- **Testes: 72** (68 → 72): +4 integração de `changePassword`.
- **Leitura de segurança Fase 2C ✅:** (ver checklist acima no plano).
```

- [ ] **Commit final + push**

```bash
git add docs/HANDOFF.md
git commit -m "docs: leitura de segurança Fase 2C + HANDOFF atualizado"
git push -u origin claude/requirements-report-plan-bz1qad
```

---

## Verificação final pré-push

Antes do push, confirmar:

```bash
cd /home/user/Vip-academia---app && pnpm test 2>&1 | tail -5
```

```bash
cd /home/user/Vip-academia---app && pnpm exec tsc --noEmit 2>&1 | head -10
```

```bash
cd /home/user/Vip-academia---app && pnpm build 2>&1 | tail -10
```

Esperado: 72 testes ✅ · 0 erros TS ✅ · build completo ✅.

---

## Self-review

**Spec coverage:**
- ✅ callbackUrl fix — Task 1
- ✅ mustChangePassword flow — Tasks 2, 3, 4, 5, 6
- ✅ /configuracoes cleanup — Task 7
- ✅ ESLint — Task 8
- ✅ Branding — Task 9
- ✅ Leitura de segurança — Task 10

**Sem placeholders:** cada step tem código completo.

**Consistência de tipos:**
- `SessionUser.mustChangePassword: boolean` — definido em Task 2, usado em Tasks 4, 5, 6
- `changePassword(session, { currentPassword?, newPassword })` — definido em Task 4, usado em Tasks 5, 6
- `changePasswordSchema` → `ChangePasswordInput` — definido em Task 3, usado em Task 5
- `forceChangePasswordSchema` — definido em Task 3, usado em Task 6
- `changePasswordAction(raw): Promise<{ error? }>` — definido em Task 5, importado em Task 7

**Dívidas intencionais registradas:**
- Open redirect de `callbackUrl`: Auth.js valida origem; confirmar em produção com `trustHost`.
- Perfil editável: virá com model `Profile` na Fase 4.
